{
  "name": "shareself-arc-socialfi",
  "version": "0.1.0",
  "private": true,
  "description": "ShareSelf - Arc Testnet SocialFi MVP (bonding curve profile shares + gated messaging)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:arc": "hardhat run scripts/deploy.ts --network arcTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.16",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
